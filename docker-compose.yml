version: "3"
services:
  php:
    build: 
      dockerfile: ./dockerfile
      context: .
    container_name: php_test_site
    hostname: ubuntu-srv4 
    ports:
      - 80:80
    restart: unless-stopped
  
#    command: CMD ["apache2-foreground"]

